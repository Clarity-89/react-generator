import { defineMessages } from "react-intl";

export default defineMessages({
    {{lowerCase name}}: {
        id: "{{lowerCase app}}.{{lowerCase name}}",
        defaultMessage: "{{properCase name}}"
    }
});
